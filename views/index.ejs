<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Courier+Prime&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css">
    <title>crypto price checker</title>
</head>
<body>
    <div class="menu">
        <% if(locals.result){ %>
            <h3><%= result.symbol %></h3>
            <% }else{ %>
                <h3>welcome</h3>
                <% } %>
        <form action="/submit" method="post">
            <!-- crypto name -->
            <select name="crypto"class="btn btn-outline-secondary" >
                <option value="BTC"class="dropdown-item">Crypto</option>
                <option value="BTC"class="dropdown-item">BTC</option>
                <option value="ETH"class="dropdown-item">ETH</option>
                <option value="SOL"class="dropdown-item">SOL</option>
              </select>
          <!-- fiat name -->
              <select name="fiat"class="btn btn-outline-secondary" >
                <option value="none"class="dropdown-item">Fiat</option>
                <option value="USD"class="dropdown-item">USD</option>
                <option value="ETH"class="dropdown-item">EUR</option>
              </select>
                <input class="btn btn-outline-secondary qty" name = "qty" type = "number" placeholder = "qty">
                <input type="submit" value="submit" class="btn btn-outline-secondary">
        </form>
          <% if(locals.result){ %>
            <div class="data">
                <h5>symbol : <%= result.symbol %></h5>
                <h5>price-24h : <%= result.price_24h %></h5>
                <h5>volume-24 : <%= result.volume_24h %></h5>
                <h5>last-trade-price : <%= result.last_trade_price %></h5>
                <h5><%= qty*result.price_24h %> <%= crypto %> for <%= qty %> <%= fiat %></h5>
              </div> 
            <% } else{%>
                <h5>check the crypto prices</h5>
                <% } %>

    </div>
</body>
</html>